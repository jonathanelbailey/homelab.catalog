apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: jaeger-tracing
  namespace: argocd
spec:
  destination:
    namespace: jaeger-tracing
    server: https://kubernetes.default.svc
  project: default
  source:
    path: devqa/services/jaeger-tracing
    repoURL: https://github.com/jonathanelbailey/homelab.catalog.git
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m0s
      limit: 2
    syncOptions:
      - CreateNamespace=true
